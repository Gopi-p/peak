# MongoDB Atlas connection string (your existing personal cluster)
MONGODB_URI=mongodb+srv://USER:PASS@cluster.mongodb.net/peak?retryWrites=true&w=majority

# Optional — identifier stored on every document (defaults to "peak")
# PEAK_OWNER=peak
